
{{ form_start(form) }}
    <div class="row">
        <div class="col-md-6">{{ form_row(form.designation) }}</div>
        <div class="col-md-6">{{ form_row(form.datePeremption) }}</div>
    </div>
    <div class="row">
        <div class="col-md-6">{{ form_row(form.imageProduit) }}</div>
        <div class="col-md-6">{{ form_row(form.quantite) }}</div>
    </div>
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button | default('Enregistrer') }}</button>
{{ form_end(form) }}